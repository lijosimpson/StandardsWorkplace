services:
  - type: web
    name: standardsworkplace-api
    env: node
    rootDir: backend
    buildCommand: npm install --prefix backend && npm run build --prefix backend
    startCommand: npm run start
    healthCheckPath: /api/health
    autoDeploy: true
